# Copy this file to `.env.local` and fill in your NEW Resend API key.
# `.env.local` is gitignored — never commit your real key.
# Used by `vercel dev` so /api/contact works locally.
RESEND_API_KEY=re_your_new_key_here
